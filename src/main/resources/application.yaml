logging:
  file:
    name: logs/app.log

server:
  port: 8080

spring:
  application:
    name: RL-Job-Scheduler
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        web-allow-others: true
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sgj
    password: password
  jpa:
    hibernate:
      ddl-auto: update

