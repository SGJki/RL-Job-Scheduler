# 未来改进计划 (Advance)

## 1. 趋势自增 ID 替代 UUID

### 问题

当前 `training_task.id` 使用 UUID 前 8 位生成：

```java
String taskId = UUID.randomUUID().toString().substring(0, 8);
```

**存在的问题：**
- 非自增，B+ 树插入随机寻址，索引性能差
- 前 8 位截断，碰撞风险高
- 无序写入导致页分裂和碎片化

### 解决方案：雪花算法（趋势自增）

```plaintext
┌──────────┬──────────────────┬──────────┬──────────┐
│ 符号位(1) │   时间戳(41bit)   │ 机器ID(10)│ 序列号(12) │
└──────────┴──────────────────┴──────────┴──────────┘
```

**优势：**
- **趋势自增**：对 B+ 树索引友好（追加写入，非随机插入）
- **分布式友好**：机器 ID 区分节点，无需协调服务
- **无单点瓶颈**：不依赖数据库自增或第三方中间件
- **唯一性保证**：同一节点同一毫秒内严格自增，不同节点互不冲突

**注意事项：**
- 依赖服务器时间，时钟回拨可能导致 ID 冲突，需做防护（如百度 Leaf 方案）
- 最多支持 1024 个节点（10bit 机器 ID）

### 实现路径

1. 引入 `snowflake-id-generator` 或自实现雪花算法
2. Worker 节点启动时从注册中心（如 Nacos）获取唯一机器 ID
3. 替换 `TrainingService.startTraining()` 中的 UUID 生成逻辑
4. 保留当前 BIGINT 自增方案作为过渡，待雪花算法稳定后可切

---

## 2. 其他待改进项

（陆续补充）
